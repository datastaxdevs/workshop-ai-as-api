tasks:
  - name: api-shell
    before: |
      cd /workspace/NLP-classifier-API
      python -m pip install --upgrade pip
      pip install -r requirements.txt
    command: |
      cd /workspace/NLP-classifier-API
      echo -e "\n\n\t\t** READY TO START... **\n\n"
      gp open README.md
github:
  prebuilds:
    master: true
    branches: true
    pullRequests: true
    pullRequestsFromForks: false
    addCheck: true
    addComment: false
    addBadge: true
    addLabel: false
ports:
  - port: 8000
    onOpen: ignore
    visibility: public
